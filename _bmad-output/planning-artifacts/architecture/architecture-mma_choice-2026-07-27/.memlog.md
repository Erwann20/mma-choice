---
scope: MMA Choice V1 — choice-driven MMA career web game; architecture spine focused on the data-driven event engine + mass content pipeline
purpose: build substrate for a solo side-project (Erwann) + AI-assisted coding
altitude: whole-system, V1 slice (initiative->features)
updated: 2026-07-27T17:48
---

- (decision) [ADOPTED] Front-only SPA, NO backend for V1 (no accounts, no server). Binds: deployment = static hosting; all logic runs client-side. Prevents: accidental server dependencies / API layer creep.
- (decision) [ADOPTED] Stack: React + TanStack Router + PrimeReact (design system). 'Simple but effective.' Bundler TBD (likely Vite). Binds all UI/routing to this trio; versions to verify on web before pinning.
- (decision) [ADOPTED] Persistence = sessionStorage. CONSEQUENCE to confirm: a Carriere is a SINGLE-SESSION artifact — closing the tab/session loses the run; no cross-session resume and no persistent meta-progression (tokens/badges) in V1. Fits 'snackable, few minutes' but diverges from D11's cross-session persistence.
- (version) Verified 2026 stable versions: React 19.2.x; TanStack Router 1.170.x (type-safe, React19 OK); Vite 8.x (default bundler, Rolldown); Zod 4.4.x (content validation). Pin exact patch via 'npm view <pkg> version' at bind time.
- (constraint) PrimeReact FLAG: primefaces/primereact repo ARCHIVED 2026-06-28 (read-only); dev moved to successor 'PrimeUI'. Last MIT release 10.9.8 works with React 19 (minor element.ref warnings) but is end-of-line. Decision needed: keep PrimeReact 10.9.8 (fine for lean V1) vs adopt PrimeUI vs other DS.
- (decision) PRNG: avoid seedrandom (unmaintained). Use pure-rand (maintained) or tiny inline mulberry32/xoshiro128 for deterministic seeded RNG in the pure engine.
- (decision) [ADOPTED] Persistence = localStorage (SUPERSEDES earlier sessionStorage decision). Carriere survives tab close; cross-session resume possible; re-opens door to persistent meta-progression (badges/Pantheon/daily) in later versions.
- (decision) [ADOPTED] State management = Zustand, with persist middleware -> localStorage. INVARIANT to enforce: the Zustand store is a THIN container; all game RULES live in the pure engine reduce(state,choice)->state. Store actions only delegate to the pure reducer and set the returned state — no game logic in the store or in components. Prevents logic leaking out of the testable core.
- (decision) [ADOPTED] Design system = PrimeUI (successor to archived PrimeReact), for longevity. Caveat: newer/less mature -> thinner docs & examples than PrimeReact; pin exact version via npm at bind time and verify React 19 + Vite integration early. Resolves the PrimeReact-vs-PrimeUI fork.
- (decision) [ADOPTED] Content format = JSON authored, validated by a single Zod schema; TS type derived via z.infer. Binds: adding content = adding validated JSON, no engine/UI code change (satisfies PRD FR-7); malformed events rejected at build/load. Enables mass/AI authoring. Prevents: content-shape drift and duplicated type definitions.
- (decision) [ADOPTED] Pure framework-agnostic engine core: reduce(GameState, Choice) -> GameState, deterministic given a seed (seeded RNG threaded through). All 3 game modes reuse the same engine. Binds: game rules never live in React/Zustand/components. Prevents: untestable logic, mode divergence, non-reproducible career score (PRD FR-14).
- (version) CORRECTED PrimeUI misnomer: no package 'primeui'. PrimeTek's React line = primereact ^11.0.0 + @primeuix/themes (peer React ^19, official Vite guide). Repo archival was governance, not deprecation. Also pinned: Zustand 5.0.x, Zod 4.4.3, pure-rand 8.4.0, React 19.2.8. Smoke-test React19.2+Vite8+primereact11 early.
- (decision) Gate fixes (amend ADs in place): AD-5 tightened -> effects/conditions target a CLOSED versioned channel enum (never raw GameState path, never engine internals rng/saveVersion/seen-flags); op closed set; stats clamped 0-100. AD-6 -> pool sorted by id before seeded draw (determinism), never-empty (filler event/phase), repeatable cooldown/cap (anti-softlock/loop). AD-7 -> state references content by id only, no embedded content objects. AD-4 -> global id-uniqueness at load; Opponent instances are engine-generated from authored archetypes (resolves two-owners hole). AD-3 -> RNG serializable state fully reconstructs generator.
- (decision) Conventions added: mobile-first (UJ-1 primary target); enforcement guards -> ESLint import-boundary forbidding engine->framework/store/ui (AD-1), no-restricted-globals Math.random in engine (AD-3), Zod content validation at build/CI (AD-4), Vitest on pure core.
- (assumption) Architecture resolves PRD OQ: localStorage (AD-7) => close-tab = resume, not loss (PRD §8 Q4/Q5). Offer to update PRD to record this.
- (event) spine finalized
- (decision) [CHANGED during dev] Design system CHANGED: PrimeReact 11 / PrimeUI ECARTE. Reason: PrimeReact v11 (PrimeTek 'next chapter') moved to a license model — even free Community use requires a registered, annually-renewed license key; without it @primereact/core injects an 'Invalid PrimeUI License' banner (shadow-root, hard to hide). Conflicts with free/zero-friction side-project. DECISION (Option C): UI = custom components (React + DESIGN.md CSS tokens); optional FREE headless lib (Radix/Ark) for a11y-sensitive primitives (dialog, bottom-sheet). Removed deps primereact/@primereact/core/@primeuix/themes/primeicons; scaffold re-verified green; JS bundle 448kB->275kB. Spine Stack + paradigm + deferred updated.
