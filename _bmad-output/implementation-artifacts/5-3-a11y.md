# Story 5.3: Plancher d'accessibilité

Status: review

## Story
As a joueur en situation de handicap, I want une interface accessible, so that je puisse jouer confortablement.

## Acceptance Criteria
1. Contraste AA, ordre de focus logique, cibles ≥ 44–48 px, `prefers-reduced-motion` respecté (NFR-10, UX-DR17). ✅
2. Un choix qui modifie des stats ⇒ variations annoncées via une live region. ✅

## Tasks / Subtasks
- [x] `ui/a11y.ts` : `describeStatChanges(prev, next)` (variations signées, libellés FR)
- [x] `ui/CareerScreen` : live region `aria-live="polite"` annonçant les deltas après chaque choix
- [x] `tokens.css` : `.sr-only`, `:focus-visible` global, `prefers-reduced-motion` global
- [x] Tests `ui/a11y.test.ts` (3)

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (Amelia)
### Completion Notes List
- Live region masquée (`.sr-only`) mise à jour par diff d'état entre deux rendus : « Réputation +8, Forme −5 » lue par le lecteur d'écran (UX-DR17).
- Focus clavier visible (contour lime) et reduced-motion global (au-delà du seul bottom-sheet) ; cibles tactiles ≥ 48 px déjà en place (boutons/cartes) ; palette Direction B conçue pour le contraste AA.
- `ResultBanner` (Story 2.3) porte déjà `role="status" aria-live` pour les résultats de combat.
- 79 tests ; `typecheck`/`lint`/`test` verts.
### File List
Nouveaux : `src/ui/a11y.ts`, `src/ui/a11y.test.ts` · Modifiés : `src/ui/CareerScreen.tsx`, `src/ui/tokens.css`

## Change Log
- 2026-07-27 — Story 5.3 : plancher a11y (live region deltas, focus, reduced-motion). 3 tests. Statut → review.
